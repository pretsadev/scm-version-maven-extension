package dev.pretsa.scm.version;

/**
 * Version type
 */
public enum VersionType {

    /**
     * Version with the same version as the current scm version
     */
    CURRENT,

    /**
     * Next version based on the current scm version
     */
    NEXT
}
